import React from "react";
import styled from "styled-components";

const Stack = styled.div`
  position: absolute;
  top: 1rem;
  right: 1rem;
`;

const IndexWrapper = styled.div`
  width: 10rem;
`;

function StackImageIndex() {
  return <Stack>StackImageIndex</Stack>;
}

export default StackImageIndex;
